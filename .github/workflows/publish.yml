name: Publish

on:
  workflow_dispatch:
    inputs:
      crate:
        description: "Crate to publish (leave as 'workspace' to publish all)"
        required: true
        type: choice
        options:
          - workspace
          - logform
          - winston_transport
          - winston_file
          - winston_daily_rotate_file
          - winston_http
          - winston_mongodb
          - winston_proxy_transport
          - winston_tracing
          - winston
      level:
        description: "Version level"
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - alpha
          - beta
          - rc
          - release

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Required to push version bumps and tags back to repo
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install cargo-release
        run: cargo install cargo-release

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          if [ "${{ inputs.crate }}" = "workspace" ]; then
            # --unpublished cannot be combined with a level, so we find crates
            # with pre-release versions ourselves and release each one explicitly.
            DEV_CRATES=$(find . -maxdepth 2 -name 'Cargo.toml' -not -path './Cargo.toml' \
              | xargs grep -l '^\[package\]' \
              | while read f; do
                  name=$(grep '^\s*name\s*=' "$f" | head -1 | sed 's/.*= *"\(.*\)"/\1/')
                  version=$(grep '^\s*version\s*=' "$f" | head -1 | sed 's/.*= *"\(.*\)"/\1/')
                  case "$version" in *-*) echo "$name" ;; esac
                done)
            if [ -z "$DEV_CRATES" ]; then
              echo "No crates with pre-release versions found."
              exit 0
            fi
            for crate in $DEV_CRATES; do
              echo "Releasing $crate..."
              cargo release ${{ inputs.level }} --package "$crate" --execute --no-confirm
            done
          else
            cargo release ${{ inputs.level }} --package ${{ inputs.crate }} --execute --no-confirm
          fi
